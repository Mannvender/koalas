import styled from "styled-components";

export default styled.input`
  background: transparent;
  border: none;
  color: #fff;
  font-size: inherit;
  font-family: inherit;
  outline: none;
`;
